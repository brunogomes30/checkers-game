export class CheckersTile{
    constructor(scene, x, y, piece = null, fragment = null){
        this.scene = scene;
        this.x = x;
        this.y = y;
        this.piece = piece;
        this.fragment = fragment;
    }
    
}