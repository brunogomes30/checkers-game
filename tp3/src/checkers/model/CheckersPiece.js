export class CheckersPiece {
    constructor(scene, color, component, position) {
        this.scene = scene;
        this.color = color;
        this.component = component;
        this.position = position;
    }
}
